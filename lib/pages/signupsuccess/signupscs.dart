import 'package:flutter/material.dart';

import 'components/body.dart';

class SignUpScsScreen extends StatelessWidget {
  static String routeName = "/signupscs";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}